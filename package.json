{
  "name": "stencil-prerender-repro",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["components", "consumer"],
  "scripts": {
    "build:components": "npm --workspace components run build",
    "start": "npm run build:components && npm --workspace consumer start"
  }
}
